{
  "name": "v1",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/analytics",
    "packages/kv",
    "packages/logger",
    "packages/ui",
    "tooling/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "clean": "git clean -xdf node_modules",
    "clean:workspaces": "turbo run clean",
    "dev": "turbo run dev",
    "dev:web": "turbo run dev --filter=@v1/web",
    "dev:app": "turbo run dev --filter=@v1/app",
    "dev:convex": "cd apps/app && bunx convex dev",
    "start:web": "turbo run start --filter=@v1/web",
    "start:app": "turbo run start --filter=@v1/app",
    "test": "turbo run test",
    "format": "biome format --write .",
    "lint": "turbo run lint && bun lint:repo",
    "lint:repo": "bunx sherif@latest",
    "lint:repo:fix": "bunx sherif@latest --fix",
    "typecheck": "turbo run typecheck",
    "migrate:clerk-production-users": "bun scripts/migrate-clerk-production-users.mjs"
  },
  "devDependencies": {
    "@biomejs/biome": "1.8.3",
    "@t3-oss/env-nextjs": "^0.11.1",
    "@v1/typescript": "workspace:*",
    "latest": "^0.2.0",
    "turbo": "^2.10.5",
    "typescript": "^5.9.3"
  },
  "packageManager": "bun@1.1.26",
  "engines": {
    "node": ">=20.9.0",
    "npm": ">=8.0.0"
  },
  "trustedDependencies": [
    "@biomejs/biome",
    "@sentry/cli",
    "protobufjs"
  ],
  "patchedDependencies": {
    "liveline@0.0.7": "patches/liveline@0.0.7.patch"
  }
}
